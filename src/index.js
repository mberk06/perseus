import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
  <React.StrictMode>
    <h1>
      WIP Perseus demo
    </h1>
  </React.StrictMode>,
  document.getElementById('root')
);
